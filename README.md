# App — ConsentChain Application Code

This directory contains all application source code:

- `contracts/` — Solidity smart contracts (ABAC engine, registries, audit log)
- `gateway/` — Policy Enforcement Gateway (Express.js middleware)
- `frontend/` — Next.js web application (patient and processor UIs)
- `scripts/` — Deployment, seeding, and utility scripts
